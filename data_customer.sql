create table data_customer (
    customer_id INTEGER,
    customer_name VARCHAR(100),
    email VARCHAR(100),
    phone_number VARCHAR(50),
    gender VARCHAR(50)
);

select * from data_customer dc;